<USER_REQUEST>
# AKAAL P5.9 — AUTHORITATIVE FROZEN CORRECTION CONTRACT

**Status:** EXTERNALLY PROVIDED / AUTHORITATIVE
**Purpose:** P5.9 forensic correction, implementation verification, hostile verification, and freeze decision.

This contract is supplied externally by the project owner/review workflow.

Repository implementation MUST be evaluated against this contract.

The repository does NOT have authority to silently redefine:

* the 38 security domains;
* Rules 1–344;
* the authoritative 28-provider/connectivity count.

Do not reconstruct replacement rules from tests.
Do not derive security domains from PermissionRegistry entries.
Do not renumber rules.
Do not rename domains.
Do not inflate provider count through aliases/managed variants.
Do not change this contract merely because current implementation differs.

If implementation contradicts this contract, report or correct the implementation.

===============================================================================
SECTION A — EXACT FROZEN 38 P5.9 SECURITY DOMAINS
=================================================

1. Enterprise Bootstrap & Root-of-Trust
2. Enterprise Identity
3. Strong Human Authentication
4. Service Identity & API Tokens
5. Machine / Workload Identity
6. Session Security
7. Dynamic RBAC
8. Canonical Permission Registry
9. Group-Based Authorization
10. Role Inheritance
11. Resource-Scoped Authorization
12. Typed ABAC
13. Deny-First Authorization
14. Separation of Duties
15. Four-Eyes / Maker-Checker
16. Multi-Stage Governance Approvals
17. Approval Integrity & Lifecycle
18. Just-in-Time Privilege
19. Zero Standing Omnipotent Bypass
20. Tenant Isolation
21. IDOR / Cross-Tenant Resistance
22. Central Authorization Engine
23. Security Revision & Cache Invalidation
24. Cryptographic Execution Authorization
25. Execution Identity Seal
26. Engine Zero Trust
27. Task/Partition Least Privilege
28. Continuous Lease & Fencing Enforcement
29. Revocation During Active Execution
30. Checkpoint/Resume Security Binding
31. Secret Isolation & JIT Resolution
32. Cryptographic Key Separation & Rotation
33. Canonicalization & Artifact Integrity
34. Tamper-Evident Security Audit
35. Threat Detection & Security Alerts
36. Secure Bootstrap, Keystore, Backup & Recovery
37. Secure Error, Input & Resource Boundaries
38. Hostile Verification & Security Proof

These names and numbers are frozen.

===============================================================================
SECTION B — EXACT FROZEN GOVERNING RULES 1–344
==============================================

## A. Scope / Zero-Fake Rules — 1–38

1. Scope-freeze P5.9 before implementation.
2. Perform a forensic repository precheck before implementation.
3. Use truthful capability classification.
4. Extend canonical authorities before creating new authorities.
5. Apply YAGNI and minimum necessary code.
6. No silent scope expansion.
7. No silent deferral of required scope.
8. Report blockers rather than bypassing them.
9. Finish the assigned subphase before moving on.
10. P5.9 must remain one cohesive implementation scope.
11. Antigravity operates under Ponytail Lite for the current owner-authorized P5.9 workflow.
12. No role or scope drift.
13. Stop after P5.9; do not begin P5.10 or P7.
14. No phase-number production filenames.
15. No cosmetic restructuring.
16. No unnecessary folders or packages.
17. Respect package and authority ownership.
18. No production mocks.
19. No dummy production implementations.
20. No demo implementation masquerading as production.
21. No placeholder success.
22. No fabricated capabilities.
23. No static fake data replacing durable state.
24. No hardcoded authentication or authorization success.
25. No admin or superuser bypass.
26. No NotImplementedError behind a capability advertised as SUPPORTED.
27. No swallowed security exceptions.
28. No fake restart durability.
29. No fake pagination guarantees.
30. No fake CDC guarantees.
31. No fake transaction guarantees.
32. No fake permission validation.
33. Security failures fail closed.
34. Unknown security state fails closed.
35. No fabricated provider or security behavior.
36. No proof inflation.
37. Negative capabilities and limitations must be reported truthfully.
38. Tests must not manufacture the capability they claim to prove.

## B. Canonical Authority Rules — 39–65

39. There must be one canonical authority per responsibility.
40. Do not create a second secret store.
41. Reuse InProcessCredentialVault for its approved JIT role if repository forensics confirm it is canonical.
42. Pipeline SQLite must not become a plaintext credential vault.
43. Reuse the canonical Separation-of-Duties authority.
44. Reuse the canonical Four-Eyes authority.
45. Reuse or extend PolicyGateEvaluator where it is the canonical governance policy authority.
46. Do not create a second governance engine.
47. Do not create a second approval authority without forensic justification.
48. Evidence #12 remains Engine-owned.
49. Pipeline must never manufacture Evidence #12.
50. Preserve the canonical migration lifecycle authority.
51. Preserve the canonical checkpoint authority.
52. Preserve the canonical retry authority.
53. Preserve the canonical validation authority.
54. Preserve the canonical planning authorities.
55. `akaal/` remains a foundational authority layer.
56. `akaalIPC` remains the communication and trust boundary.
57. `akaalPipeline` remains the operational motherboard for orchestration and governance.
58. `akaalEngine` remains the physical execution authority.
59. UI must not become an execution authority.
60. IPC must not become the business authorization engine.
61. Pipeline must not become a physical database execution engine.
62. Engine must not become lifecycle or governance authority.
63. Preserve Caller → akaalIPC → akaalPipeline → akaalEngine.
64. Cross-layer interaction must use defined ports/adapters.
65. No cross-layer bypasses.

## C. IPC Rules — 66–79

66. IPC requests are typed.
67. IPC responses are typed.
68. IPC payloads are JSON-safe.
69. Preserve command/query distinction.
70. Queries must not hide mutations.
71. Commands carry authenticated context.
72. External callers cannot forge trusted internal context.
73. SYSTEM identity/type cannot be externally supplied.
74. IPC errors must not expose secrets.
75. Errors crossing IPC are structured and sanitized.
76. Actor, correlation, and causation context must propagate correctly.
77. Cursors are validated and opaque where applicable.
78. Subscriptions consume Pipeline-authoritative truth.
79. UI/IPC disconnection must not cancel backend execution.

## D. Planning / Lifecycle Rules — 80–96

80. Pipeline owns the durable ExecutionPlan.
81. Engine must not create a competing durable plan authority.
82. PlanCompiler owns semantic execution strategy.
83. GraphCompiler owns DAG/lifecycle compilation.
84. Engine Runtime owns physical worker execution.
85. Preserve the PlanCompiler → GraphCompiler → Engine Runtime three-stage model.
86. ExecutionMode/MigrationMode parity is required.
87. Assertions must prevent execution-mode drift.
88. Pipeline owns authoritative business lifecycle state.
89. Pipeline owns business orchestration state.
90. Engine owns physical durability truth.
91. Engine owns physical partition watermarks.
92. Engine owns physical CDC positions.
93. Engine owns physical WAL/checkpoint truth.
94. Pipeline must not fabricate physical checkpoints.
95. Recovery reconciles authoritative physical truth forward into Pipeline state.
96. A committed physical batch must not be blindly replayed.

## E. Admission Rules — 97–106

97. Durable admission must occur before physical mutation.
98. Authorization must occur before dispatch.
99. Lifecycle state must be validated before dispatch.
100. Governance gates must be satisfied before dispatch.
101. Operation journal state must be durable before dispatch.
102. Required audit/outbox state must participate transactionally in admission.
103. Strictly read-only probes may execute before plan admission only when physically read-only.
104. Read-only probe results used by planning must be fingerprinted and durably committed as required.
105. Failed admission must result in zero physical mutation.
106. Dispatch-before-admission is a blocker.

## F. Execution Identity Seal Rules — 107–114

107. Execution authorization uses a structured Execution Identity Seal.
108. The seal includes `seal_version`.
109. The seal binds tenant, workspace, project, and migration identity.
110. The seal binds plan identity and plan revision.
111. The seal binds execution mode, source fingerprint, and target fingerprint.
112. The seal binds selection fingerprint, configuration fingerprint, and initialization fingerprint.
113. The seal binds approval fingerprint.
114. Material identity changes, including fencing epoch changes, must change/invalidate the seal as applicable.

## G. Continuous Fencing Rules — 115–124

115. Fencing epochs are monotonic.
116. Engine independently verifies fencing state.
117. Ingress-only fencing is insufficient.
118. Fence state travels with the physical write context.
119. Fence validity is rechecked at physical mutation boundaries.
120. Physical commit validates fencing.
121. DDL mutation validates fencing.
122. A stale worker must not successfully commit.
123. Stale fencing causes rollback/error where provider semantics permit.
124. Fencing must defend against zombie-worker and TOCTOU stale-writer behavior.

## H. Idempotency Rules — 125–132

125. Pipeline command idempotency is authoritative at the command level.
126. An idempotency key binds the command payload fingerprint.
127. Equivalent replay returns/reconciles to the established outcome.
128. Conflicting reuse of an idempotency key fails closed.
129. Engine task idempotency is distinct from Pipeline command idempotency.
130. Engine task retries use canonical invocation identity.
131. Retries must not duplicate already committed physical batches.
132. Command idempotency and task idempotency remain separate authorities.

## I. Engine Zero-Trust Rules — 133–142

133. Engine does not trust an upstream authorization assertion merely because Pipeline supplied it.
134. Engine independently verifies cryptographic execution authorization.
135. Engine verifies tenant identity.
136. Engine verifies migration/execution identity.
137. Engine verifies execution mode.
138. Engine verifies plan/intent/seal identity.
139. Engine verifies authorization validity, revision, and expiry.
140. Engine verifies task scope.
141. Engine verifies fencing epoch/state.
142. Unverifiable execution authorization is denied.

## J. Validation-Only / M8 Rules — 143–147

143. M8 validation-only execution is structurally read-only.
144. Planner must not emit mutating M8 work.
145. Pipeline must not authorize mutating M8 operations.
146. Gateway rejects mutating M8 requests.
147. Physical writer/read-only connection enforcement is used where the provider supports it.

## K. Identity / RBAC Rules — 148–170

148. Principals are durable.
149. Principal IDs are immutable.
150. Principal type is explicit.
151. HUMAN identity is supported.
152. SERVICE identity is supported.
153. MACHINE/WORKLOAD identity is supported.
154. SYSTEM identity is internal-only.
155. Principals can be disabled/revoked.
156. Tenant groups are durable.
157. Group membership is durable.
158. Nested groups, if supported, must be deterministic and cycle-safe; if deliberately unsupported, that limitation is explicit and enforced.
159. PermissionRegistry is canonical.
160. Unknown permissions deny.
161. Roles are dynamic and durable.
162. Role inheritance is deterministic and cycle-safe.
163. Role-permission relationships are persisted.
164. A role grant targets a principal or group according to the canonical exclusive grant model.
165. Role grants are resource-scoped.
166. CentralAuthorizationEngine is the canonical authorization decision authority.
167. Group-derived authorization flows through canonical RBAC.
168. Expired grants deny.
169. Revoked grants deny.
170. Authorization decisions are reproducible from authoritative state.

## L. ABAC / Tenancy Rules — 171–187

171. ABAC evaluation is typed and deterministic.
172. Subject attributes come from authoritative subject state.
173. Resource attributes come from authoritative resource state.
174. Environment attributes have defined provenance.
175. Action is represented canonically.
176. Missing required attributes fail closed.
177. Undefined type coercion is prohibited.
178. Explicit DENY overrides ALLOW.
179. Tenant identity binds commands.
180. Tenant identity binds queries.
181. Repositories enforce tenant scoping.
182. Subscription/event access enforces tenant isolation.
183. Journal records are tenant-bound.
184. Artifacts and Evidence references validate tenant identity.
185. Engine gateway validates tenant identity.
186. UI filtering is not a security boundary.
187. Cross-tenant probes fail closed.

## M. Governance Rules — 188–205

188. Governance policies are durable.
189. Governance policies are versioned.
190. Governance evaluation is deterministic.
191. Governance decisions bind an intent fingerprint.
192. Material policy changes invalidate stale authorization where required.
193. Approvals are durable.
194. Sequential approval is supported where required.
195. Quorum approval is supported where required.
196. Approvals have expiry semantics.
197. REJECTED is a first-class approval outcome.
198. Approval withdrawal/revocation is supported.
199. Material plan changes invalidate stale approvals.
200. Maker/checker separation is enforced.
201. Self-approval is prohibited where policy requires separation.
202. Incompatible/toxic roles are enforced.
203. Self-escalation is prohibited.
204. Privileged operations can require stronger authentication/JIT authorization.
205. Pipeline owns business governance.

## N. Secret Rules — 206–218

206. Durable models use opaque secret references.
207. Plaintext database passwords must not be durably stored.
208. IPC must not expose plaintext secrets.
209. Logs must not expose secrets.
210. Events must not expose secrets.
211. Evidence must not contain credentials.
212. Engine resolves required secrets just in time.
213. Secret resolution uses real provider contracts.
214. Secret rotation/version identity is preserved where applicable.
215. Plaintext secret lifetime is minimized.
216. Memory cleanup is best effort; do not falsely claim guaranteed Python zeroization.
217. Secret-resolution failure fails closed.
218. There is no hardcoded secret fallback.

## O. Authentication / Session Rules — 219–234

219. Human passwords use a salted adaptive KDF.
220. Stored password representation uses a versioned/modular envelope.
221. Security configuration enforces immutable minimum KDF floors.
222. KDF capability is checked.
223. Unsupported/unavailable required KDF behavior fails closed rather than silently downgrading existing credentials.
224. Password verification considers timing safety.
225. Authentication has abuse controls.
226. Lockout behavior is policy-driven within secure bounds.
227. Successful authentication resets appropriate failed-authentication counters.
228. Sessions are durable.
229. Sessions enforce absolute expiry.
230. Sessions enforce idle timeout.
231. Sessions are revocable.
232. Replay/nonce protection is used where required.
233. Relevant signing/encryption keys support rotation.
234. Restart must not resurrect revoked sessions or credentials.

## P. Cryptography Rules — 235–242

235. Canonicalization uses a frozen deterministic contract.
236. Cryptographic artifacts use domain/version separation.
237. Keys are purpose-separated.
238. Cryptographic artifacts identify the relevant `key_id`.
239. Keys have explicit lifecycle state.
240. Use established approved cryptographic primitives.
241. Immutable identity/fingerprint material uses deterministic SHA-256 where specified.
242. Security revision/anti-rollback state is monotonic.

## Q. Evidence Rules — 243–250

243. Evidence remains Authority #12.
244. Evidence #12 occurs after Validation #11.
245. Engine is the sole creator of Evidence #12.
246. Pipeline may consume/verify/reference Evidence #12.
247. Pipeline must not manufacture Evidence #12.
248. Evidence reflects physical/provenance/Merkle truth as applicable.
249. Evidence #12 must not become reporting or governance.
250. Cutover/readiness may consume validated Evidence #12.

## R. Completion Rules — 251–256

251. Engine reports physical execution outcome.
252. Validation reports parity/correctness truth.
253. Evidence reports physical provenance truth.
254. Pipeline reconciles those authorities.
255. Only Pipeline declares overall migration COMPLETED.
256. Worker/task success alone is not overall completion.

## S. Cancellation Rules — 257–265

257. UI disconnect does not cancel execution.
258. Cancellation is an explicit authorized backend operation.
259. Cancellation stops new scheduling.
260. Active work reaches a defined safe boundary.
261. Physical state is committed or rolled back according to canonical semantics.
262. Required physical checkpoint state is persisted.
263. Leases are released or superseded/fenced.
264. Lifecycle transitions to the appropriate cancelled/paused state.
265. Cancellation/restart semantics are restart-safe.

## T. Audit / Detection Rules — 266–279

266. Security events are structured.
267. Security events carry provenance.
268. Security audit state is durable.
269. Audit ordering/integrity is verifiable within its stated threat model.
270. Privilege changes are auditable.
271. Approval activity is auditable.
272. Authentication failures produce appropriate security signal.
273. Cross-tenant attempts are detectable.
274. Replay attempts are detectable.
275. Execution-seal violations are detectable.
276. Fence/zombie-worker violations are detectable.
277. Privilege-escalation sequences are detectable where specified.
278. Security alerts have severity.
279. Security events can feed provider-neutral SIEM/integration boundaries.

## U. Security / Performance Rules — 280–291

280. Do not add security controls merely to increase feature count.
281. Security remains organized around coherent canonical authorities.
282. JIT privilege minimizes standing privilege.
283. Containment should be targeted rather than globally disruptive where safe.
284. Security state includes anti-rollback protection where required.
285. Authorization caches invalidate when authoritative security revisions change.
286. Administrative security operations handle concurrency safely.
287. Required security dependencies fail closed.
288. Abuse controls must not become an accidental migration-throughput bottleneck.
289. Defaults are secure.
290. Security invariants are centralized rather than scattered as magic checks.
291. Hot-path security checks are bounded and avoid unnecessary global contention.

## V. Wails / Product Boundary Rules — 292–300

292. Security implementation is backend-first.
293. Wails remains a thin native shell.
294. Wails/frontend communicates through typed IPC.
295. UI must not implement canonical RBAC.
296. UI must not become authorization authority.
297. UI must not become lifecycle truth.
298. Avoid exposing raw secrets to JavaScript.
299. Frontend receives sanitized security state/errors.
300. Backend setup/admin contracts remain stable and consumable by Wails.

## W. Hostile Verification Rules — 301–325

301. Hostile verification covers authentication bypass.
302. Hostile verification covers session replay/revocation.
303. Hostile verification covers service-token compromise/replay.
304. Hostile verification covers SYSTEM identity spoofing.
305. Hostile verification covers RBAC escalation.
306. Hostile verification covers malformed/bypass ABAC attempts.
307. Hostile verification covers tenant/IDOR attacks.
308. Hostile verification covers SoD/self-approval attacks.
309. Hostile verification covers stale/modified approval artifacts.
310. Hostile verification covers execution-authorization forgery/tampering.
311. Hostile verification covers revocation races.
312. Hostile verification covers administrative concurrency.
313. Hostile verification covers crash/restart security state.
314. Hostile verification covers fail-closed dependency loss.
315. Hostile verification covers M8 mutation attacks.
316. Hostile verification covers idempotency replay/conflict.
317. Regression verification is required.
318. Python compile/import verification is required.
319. Backend regression suites are required.
320. Frontend build/regression is required if frontend is affected.
321. Wails build/check is required if Wails is affected.
322. `git diff --check` is required.
323. Unauthorized diff must be checked.
324. Duplicate-authority audit is required.
325. Claimed proof level must never exceed actual evidence.

## X. Review / Freeze Rules — 326–333

326. P5.9 receives an independent hostile assessment.
327. Report genuine defects only.
328. Do not manufacture suggestions merely to appear critical.
329. Required defects are consolidated into a correction pass.
330. Hostile verification is rerun after corrections.
331. Relevant regressions are rerun after corrections.
332. Freeze requires architecture and evidence consistency.
333. Passing tests alone do not prove architecture correctness.

## Y. Git Rules — 334–344

334. Run `git status`.
335. Run `git add -A`.
336. Run `git status` again after staging.
337. Commit the verified correction.
338. Push to `origin main`.
339. Run `git pull --rebase origin main`.
340. Run final `git status`.
341. Run `git branch -vv`.
342. Run `git rev-parse HEAD`.
343. Run `git rev-parse origin/main`.
344. Final repository state must be clean and `HEAD == origin/main`.

===============================================================================
SECTION C — AUTHORITATIVE CONNECTIVITY CONTRACT
===============================================

The authoritative AKAAL connectivity count for this frozen work is:

**28 unique physical providers/connectors.**

Do NOT report this as 30.

Important distinction:

* unique physical connectors/providers determine the authoritative count;
* managed-service aliases/profiles/distribution variants must not artificially inflate the physical connector count.

The canonical physical-provider baseline to verify against the repository is:

RELATIONAL — 7

1. Oracle
2. PostgreSQL
3. MySQL
4. MariaDB
5. Microsoft SQL Server
6. IBM Db2
7. SQLite

WAREHOUSE / LAKEHOUSE — 4
8. Snowflake
9. BigQuery
10. Amazon Redshift
11. Databricks / Delta Lake

NOSQL / SPECIALIZED — 8
12. MongoDB
13. Cassandra
14. ScyllaDB
15. Neo4j
16. Redis
17. KeyDB
18. Elasticsearch
19. OpenSearch

STREAMING — 4
20. Apache Kafka
21. Amazon Kinesis
22. Azure Event Hubs
23. Google Cloud Pub/Sub

DISTRIBUTED / OBJECT STORAGE — 5
24. HDFS
25. Amazon S3
26. Google Cloud Storage
27. Azure Blob Storage
28. MinIO

TOTAL = 28 UNIQUE PHYSICAL PROVIDERS/CONNECTORS.

Kafka-compatible managed/distribution identities such as Confluent or Amazon MSK must not automatically become additional unique physical connectors if they use the canonical Kafka physical connector with managed-service/profile configuration.

Likewise, cloud-managed database variants/profiles must not inflate the unique physical connector count merely because they have distinct deployment identities.

Repository forensics must determine how the 28 canonical physical connectors correspond to registered identities, aliases, profiles, strategies, or adapters.

Do not delete valid aliases/profiles merely to make the number 28.

Instead distinguish:

PHYSICAL CONNECTOR
MANAGED PROFILE
ALIAS
DISTRIBUTION PROFILE
PROVIDER-SPECIFIC CONFIGURATION

The authoritative physical count remains 28.

===============================================================================
SECTION D — FROZEN SECURITY CHAIN
=================================

The P5.9 security chain is:

IDENTITY
→ AUTHENTICATION
→ SESSION
→ TENANT
→ RBAC
→ RESOURCE SCOPE
→ ABAC
→ SoD
→ GOVERNANCE
→ APPROVAL
→ JIT PRIVILEGE
→ EXECUTION AUTHORIZATION
→ EXECUTION IDENTITY SEAL
→ ENGINE ZERO TRUST
→ TASK LEAST PRIVILEGE
→ CONTINUOUS FENCING
→ PHYSICAL MUTATION
→ CHECKPOINT
→ VALIDATION #11
→ EVIDENCE #12
→ AUDIT + DETECTION

The intended P5.9 differentiating property is:

“A dynamically authorized human causes an approved, intent-bound,
cryptographically authorized, task-scoped, independently verified and
continuously fenced physical execution whose resulting physical truth can be
validated, evidenced and audited.”

Do not claim this property beyond the level physically proven.

===============================================================================
SECTION E — EXECUTION IDENTITY SEAL
===================================

The frozen Execution Identity Seal contains `seal_version` plus these 14
identity dimensions:

1. tenant
2. workspace
3. project
4. migration
5. plan
6. plan revision
7. execution mode
8. source fingerprint
9. target fingerprint
10. selection fingerprint
11. configuration fingerprint
12. initialization fingerprint
13. approval fingerprint
14. fencing epoch

Any material identity change must alter/invalidate the seal as defined by the
canonical security authority.

===============================================================================
SECTION F — PROOF LEVELS
========================

Only use:

IMPLEMENTED
UNIT_PROVEN
INTEGRATION_PROVEN
LIVE_PROVEN

Definitions:

IMPLEMENTED
= production-reachable implementation physically exists.

UNIT_PROVEN
= production authority is verified at unit/component scope without pretending
an external real system was exercised.

INTEGRATION_PROVEN
= multiple real AKAAL production authorities/components are exercised together
through their actual integration boundary.

LIVE_PROVEN
= AKAAL physically exercises the capability against the relevant real external
database/service/provider/runtime.

Do not invent replacement proof levels.

A registry declaring `UNIT_PROVEN` does not by itself prove a physical
provider operation.

===============================================================================
SECTION G — CONTINUOUS / DISTRIBUTED FENCING REQUIREMENT
========================================================

Ingress fencing alone is insufficient.

Local process state alone is insufficient for a distributed guarantee.

Local SQLite WAL coordination may prove local/multiprocess durable coordination
but must not be described as distributed consensus.

For a true distributed/network-partition fencing claim, prove:

Worker A obtains epoch N.
Worker B supersedes ownership with epoch N+1.
Worker A becomes stale/partitioned but can still reach the physical target.
Worker A attempts an irreversible physical mutation/commit.
A shared/target-visible authoritative mechanism prevents Worker A from
successfully committing stale work.

Where a provider cannot physically guarantee this property, classify the
capability truthfully and fail closed for modes requiring the stronger
guarantee.

===============================================================================
SECTION H — EVIDENCE #12
========================

Validation is Authority #11.

Evidence is Authority #12.

Evidence #12:

* follows Validation #11;
* is created only by akaalEngine;
* represents physical provenance/evidence;
* may be consumed/referenced by Pipeline;
* must not be manufactured by Pipeline;
* must not become reporting;
* must not become governance.

===============================================================================
SECTION I — PYTHON SECRET-MEMORY TRUTH
======================================

Do not claim guaranteed secret-memory zeroization in standard Python.

Allowed truthful statement:

AKAAL minimizes plaintext secret lifetime and may perform best-effort in-place
overwrite of mutable buffers plus prompt reference release/eviction.

CPython may retain copies because of immutable objects, allocator behavior,
garbage collection, internal/runtime copies, libraries, or operating-system
behavior.

Therefore guaranteed complete memory eradication must not be claimed unless a
separate physically proven mechanism provides it.

===============================================================================
SECTION J — AUDIT THREAT-MODEL TRUTH
====================================

A local SHA-256 hash-chained SQLite ledger can provide tamper evidence for
interior modification, ordering corruption, deletion/gaps where the remaining
chain exposes them.

Do not claim independent protection against complete database rollback,
replacement with an earlier internally valid snapshot, or valid-tail
truncation unless an independent trusted anchor/root-of-trust/monotonic
external state physically exists.

===============================================================================
SECTION K — FINAL FORENSIC INSTRUCTION
======================================

Now continue the existing P5.9 correction pass.

Do not produce another superficial status report.

Use this contract as the authoritative comparison target.

Specifically close the remaining blockers:

1. Map implementation to the EXACT 38 frozen domains above.
2. Account individually for the EXACT Rules 1–344 above.
3. Individually classify every full-regression failure/error with evidence.
4. Correct or truthfully downgrade distributed/network-partition fencing.
5. Reconcile repository registrations with the authoritative 28 physical
   connectors without deleting legitimate aliases/profiles.
6. Trace physical fencing through actual provider mutation/finalization paths.
7. Complete active-execution revocation through the real Engine execution path.
8. Complete checkpoint/resume hostile binding through the real resume authority.
9. Complete the full duplicate-authority audit.
10. Audit hostile tests for false-positive broad exception handling, especially
    after discovering that HOSTILE-ATK-61 previously passed through
    AttributeError under broad `pytest.raises(Exception)`.
11. Preserve truthful Python-memory and audit-ledger limitations.
12. Re-run required security, hostile, regression, compile/import, architecture,
    diff, and Git verification.

Do not mark something GREEN because metadata or documentation says it is
supported.

Do not mark something GREEN merely because a test raised some arbitrary
exception.

Do not change tests to hide genuine implementation failures.

Do not manufacture live-provider evidence.

Do not begin P5.10.

A blocker becomes GREEN only when the exact frozen requirement above is
implemented or truthfully satisfied and backed by the appropriate physical
evidence.

</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-08-28T13:43:29+05:30.
</ADDITIONAL_METADATA>