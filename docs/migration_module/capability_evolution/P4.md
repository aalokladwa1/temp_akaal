# Capability Evolution — Phase P4 (CDC & Continuous Synchronization)

**Status:** Future Phase Placeholder  
**Target Phase:** P4  

---

## Intended Purpose

This document will record all capabilities introduced during Phase P4:
- Native CDC transaction log mining across Oracle (LogMiner), PostgreSQL (Logical Replication / pgoutput), MySQL (Binlog), and MSSQL (CDC / Change Tracking).
- Sub-second continuous synchronization pipeline (`WF-015`, `WF-016`).
- LSN transaction stream lag counters and CDC telemetry indicators in Monitoring UI.
