# Capability Evolution — Phase P3 (Change Data Capture & CDC Synchronization)

**Status:** P3.1 Complete (Canonical CDC Architecture, Consistency Boundary & Domain Foundation)  
**Target Phase:** P3  

---

## Evolution History

### P3.1 — Canonical CDC Architecture, Consistency Boundary & Domain Foundation
- **Polymorphic Engine-Specific Source Positions ([`positions.py`](file:///a:/temp_akaal/akaal/cdc/domain/positions.py))**: Established polymorphic, engine-specific source position abstractions for PostgreSQL (`PostgresLSNPosition`), MySQL (`MySQLGTIDPosition`), Oracle (`OracleSCNPosition`), SQL Server (`MSSQLChangePosition`), and MongoDB (`MongoDBOpLogPosition`) with monotonic comparison and string/dictionary serialization.
- **Identity-Bound Event & Transaction Models ([`events.py`](file:///a:/temp_akaal/akaal/cdc/domain/events.py))**: Established `CDCEventIdentity`, `CDCEvent`, and `CDCTransaction` bound to `migration_id`, `job_id`, `run_id`, and `cdc_session_id`. Provides `to_data_safe_dict()` for secrets-redacted diagnostics.
- **Initial Load -> CDC Consistency Boundary ([`consistency.py`](file:///a:/temp_akaal/akaal/cdc/domain/consistency.py))**: Formalized `CDCConsistencyBoundary` enforcing that `cdc_capture_start_position <= initial_load_snapshot_position`, preventing change loss between bulk transport and continuous CDC.
- **Acknowledgement & Session Lifecycle State Machines ([`lifecycle.py`](file:///a:/temp_akaal/akaal/cdc/domain/lifecycle.py))**: Defined `CDCAckState` (`CAPTURED` -> `DURABLY_BUFFERED` -> `APPLYING` -> `APPLIED` -> `CHECKPOINTED` -> `ACKNOWLEDGED`) and `CDCSessionStateMachine` enforcing legal state transitions.
- **Durable Checkpoints & HMAC Integrity ([`durability.py`](file:///a:/temp_akaal/akaal/cdc/domain/durability.py))**: Created `CDCCheckpoint` with SHA-256 HMAC integrity hashes.
- **Failure Taxonomy & Exceptions ([`errors.py`](file:///a:/temp_akaal/akaal/cdc/domain/errors.py))**: Defined `CDCFailureCategory` (`RETRYABLE`, `PAUSABLE`, `BLOCKING`, `TERMINAL`, `DATA_INTEGRITY_RISK`) and `CDCExecutionError`.
- **CDC Monitoring Telemetry Extension ([`telemetry.py`](file:///a:/temp_akaal/akaal/cdc/domain/telemetry.py))**: Created `CDCMonitoringDTO` extending `CentralStateStore` monitoring DTOs.
- **24 Dedicated Acceptance Tests ([`test_p3_1_canonical_cdc_domain_foundation.py`](file:///a:/temp_akaal/tests/unit/cdc/test_p3_1_canonical_cdc_domain_foundation.py))**: 24/24 passing cleanly.
