# Capability Evolution — Phase P2 (Schema Intelligence & Automated Translation)

**Status:** In Progress (P2.4 Structural DDL Complete)  
**Target Phase:** P2  

---

## Evolution History

### P2.2 — Canonical Schema Model & Universal Metadata Normalization
- **Canonical Schema Domain Model ([`models.py`](file:///a:/temp_akaal/akaal/schema/domain/models.py))**: Established database-agnostic domain representations for `CanonicalSchemaModel`, `CanonicalTable`, `CanonicalColumn`, `CanonicalPrimaryKey`, `CanonicalForeignKey`, `CanonicalUniqueConstraint`, `CanonicalCheckConstraint`, `CanonicalDefault`, `CanonicalIndex`, `CanonicalSequence`, `CanonicalIdentity`, `CanonicalPartition`, `CanonicalView`, `CanonicalMaterializedView`, `CanonicalProcedure`, `CanonicalFunction`, and `CanonicalTrigger`.
- **Four-Adapter Normalization**: Added `get_canonical_schema(schema_name)` across `OracleAdapter`, `PostgreSQLAdapter`, `MySQLAdapter`, and `MSSQLAdapter` to map vendor catalog metadata into standardized `CanonicalSchemaModel` instances.
- **Workflow Pipeline Refactoring**: Updated `SchemaExecutionStep` in [`migration_steps.py`](file:///a:/temp_akaal/akaal/workflow/steps/migration_steps.py) to construct `CanonicalTable` and `CanonicalColumn` domain models before target DDL execution.
- **Deterministic Fingerprinting**: Implemented `compute_schema_fingerprint()` generating SHA-256 structural schema signatures.

---

### P2.3 — Universal Datatype System & Rulebook Standardization
- **Canonical Datatype System ([`types.py`](file:///a:/temp_akaal/akaal/schema/domain/types.py))**: Implemented database-agnostic `CanonicalType` and `CanonicalTypeCategory` enum covering 22 semantic categories (`INTEGER`, `DECIMAL`, `FLOAT`, `BOOLEAN`, `CHAR`, `VARCHAR`, `TEXT`, `BINARY`, `VARBINARY`, `BLOB`, `DATE`, `TIME`, `TIMESTAMP`, `TIMESTAMPTZ`, `UUID`, `JSON`, `JSONB`, `XML`, `INTERVAL`, `ENUM`, `ARRAY`, `UNKNOWN`).
- **Conversion Safety Classification**: Implemented `ConversionSafety` classification (`EXACT`, `SAFE`, `POTENTIALLY_LOSSY`, `LOSSY`, `UNSUPPORTED`, `VENDOR_SPECIFIC`) attached to every target emission for downstream P2.7 risk analysis.
- **Universal Type Registry & Rulebook ([`type_registry.py`](file:///a:/temp_akaal/akaal/schema/domain/type_registry.py))**: Centralized 2-step type conversion pipeline: `Source Native Type → Source Adapter Normalizer → CanonicalType → Target Type Emitter → Target Native Type`.

---

### P2.3.1 — Datatype Semantic Acceptance Audit & Hardening
- **MySQL `TINYINT(1)` Semantic Hardening**: Hardened `TINYINT(1)` to preserve `INTEGER(bits=8)` by default unless explicit boolean evidence (`is_boolean=True`) is provided in metadata.
- **Oracle Parameter Ambiguity & Negative Scale**: Fixed type parameter parsing regex (`r"\(([\d\s,\-]+)\)"`) to support Oracle negative scale (`NUMBER(5,-2)`).
- **MSSQL `ROWVERSION` / `TIMESTAMP` Hardening**: Verified `ROWVERSION` normalizes to `VARBINARY(length=8, extra={"mssql_rowversion": True})` and emits `ConversionSafety.VENDOR_SPECIFIC` with warning when targeting PostgreSQL `BYTEA`.
- **Unsigned 64-Bit Range Overflow Protection**: Mapped MySQL `BIGINT UNSIGNED` to PostgreSQL `NUMERIC(20,0)` to avoid signed 64-bit integer overflow (`ConversionSafety.SAFE`).
- **Dynamic Plugin Extensibility**: Added `register_normalizer(engine, func)` and `register_emitter(engine, func)` on `CanonicalTypeRegistry`.

---

### P2.4 — Universal Structural DDL Generation Architecture
- **Universal DDL Authority ([`ddl_emitter.py`](file:///a:/temp_akaal/akaal/schema/domain/ddl_emitter.py))**: Established single, database-agnostic DDL generation authority `UniversalDDLAuthority` producing strongly-typed `StructuredDDLArtifact` instances for tables, columns, single/composite primary keys, single/composite foreign keys, unique constraints, check constraints, default expressions, indexes, sequences, identity/auto-increment columns, and partitions.
- **Target DDL Emitter Matrix**: Implemented target emitters for `PostgreSQLDDLEmitter`, `OracleDDLEmitter`, `MySQLDDLEmitter`, and `MSSQLDDLEmitter`.
- **Workflow Pipeline Refactoring**: Updated `SchemaExecutionStep` in [`migration_steps.py`](file:///a:/temp_akaal/akaal/workflow/steps/migration_steps.py) to generate target structural DDL using `UniversalDDLAuthority.emit_table_ddl()`.
- **Identifier Quoting & Reserved Keywords**: Handled PostgreSQL `"schema"."table"`, Oracle `"SCHEMA"."TABLE"`, MySQL `` `schema`.`table` ``, and MSSQL `[schema].[table]`.
- **Plugin Extensibility Guarantee**: Added `register_emitter(engine, emitter)` on `UniversalDDLAuthority`. DB #5 (IBM DB2) trace proves adding Database #5 or #50 requires 0 modifications to `WorkflowEngine` or core replication code.
