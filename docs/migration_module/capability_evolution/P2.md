# Capability Evolution — Phase P2 (Schema Intelligence & Automated Translation)

**Status:** Complete & Frozen (P2.11 Reports, Validation Results, Certification & Evidence UI Complete & Frozen)  
**Target Phase:** P2  

---

## Evolution History

### P2.2 — Canonical Schema Model & Universal Metadata Normalization
- **Canonical Schema Domain Model ([`models.py`](file:///a:/temp_akaal/akaal/schema/domain/models.py))**: Established database-agnostic domain representations for `CanonicalSchemaModel`, `CanonicalTable`, `CanonicalColumn`, `CanonicalPrimaryKey`, `CanonicalForeignKey`, `CanonicalUniqueConstraint`, `CanonicalCheckConstraint`, `CanonicalDefault`, `CanonicalIndex`, `CanonicalSequence`, `CanonicalIdentity`, `CanonicalPartition`, `CanonicalView`, `CanonicalMaterializedView`, `CanonicalProcedure`, `CanonicalFunction`, and `CanonicalTrigger`.
- **Four-Adapter Normalization**: Added `get_canonical_schema(schema_name)` across `OracleAdapter`, `PostgreSQLAdapter`, `MySQLAdapter`, and `MSSQLAdapter` to map vendor catalog metadata into standardized `CanonicalSchemaModel` instances.
- **Workflow Pipeline Refactoring**: Updated `SchemaExecutionStep` in [`migration_steps.py`](file:///a:/temp_akaal/akaal/workflow/steps/migration_steps.py) to construct `CanonicalTable` and `CanonicalColumn` domain models before target DDL execution.
- **Deterministic Fingerprinting**: Implemented `compute_schema_fingerprint()` generating SHA-256 structural schema signatures.

---

### P2.3 — Universal Datatype System & Rulebook Standardization
- **Canonical Datatype System ([`types.py`](file:///a:/temp_akaal/akaal/schema/domain/types.py))**: Implemented database-agnostic `CanonicalType` and `CanonicalTypeCategory` enum covering 22 semantic categories (`INTEGER`, `DECIMAL`, `FLOAT`, `BOOLEAN`, `CHAR`, `VARCHAR`, `TEXT`, `BINARY`, `VARBINARY`, `BLOB`, `DATE`, `TIME`, `TIMESTAMP`, `TIMESTAMPTZ`, `UUID`, `JSON`, `JSONB`, `XML`, `INTERVAL`, `ENUM`, `ARRAY`, `UNKNOWN`).
- **Conversion Safety Classification**: Implemented `ConversionSafety` classification (`EXACT`, `SAFE`, `POTENTIALLY_LOSSY`, `LOSSY`, `UNSUPPORTED`, `VENDOR_SPECIFIC`) attached to every target emission for downstream P2.7 risk analysis.
- **Universal Type Registry & Rulebook ([`type_registry.py`](file:///a:/temp_akaal/akaal/schema/domain/type_registry.py))**: Centralized 2-step type conversion pipeline: `Source Native Type → Source Adapter Normalizer → CanonicalType → Target Type Emitter → Target Native Type`.

---

### P2.3.1 — Datatype Semantic Acceptance Audit & Hardening
- **MySQL `TINYINT(1)` Semantic Hardening**: Hardened `TINYINT(1)` to preserve `INTEGER(bits=8)` by default unless explicit boolean evidence (`is_boolean=True`) is provided in metadata.
- **Oracle Parameter Ambiguity & Negative Scale**: Fixed type parameter parsing regex (`r"\(([\d\s,\-]+)\)"`) to support Oracle negative scale (`NUMBER(5,-2)`).
- **MSSQL `ROWVERSION` / `TIMESTAMP` Hardening**: Verified `ROWVERSION` normalizes to `VARBINARY(length=8, extra={"mssql_rowversion": True})` and emits `ConversionSafety.VENDOR_SPECIFIC` with warning when targeting PostgreSQL `BYTEA`.
- **Unsigned 64-Bit Range Overflow Protection**: Mapped MySQL `BIGINT UNSIGNED` to PostgreSQL `NUMERIC(20,0)` to avoid signed 64-bit integer overflow (`ConversionSafety.SAFE`).
- **Dynamic Plugin Extensibility**: Added `register_normalizer(engine, func)` and `register_emitter(engine, func)` on `CanonicalTypeRegistry`.

---

### P2.4 — Universal Structural DDL Generation Architecture
- **Universal DDL Authority ([`ddl_emitter.py`](file:///a:/temp_akaal/akaal/schema/domain/ddl_emitter.py))**: Established single, database-agnostic DDL generation authority `UniversalDDLAuthority` producing strongly-typed `StructuredDDLArtifact` instances.
- **Target DDL Emitter Matrix**: Implemented target emitters for `PostgreSQLDDLEmitter`, `OracleDDLEmitter`, `MySQLDDLEmitter`, and `MSSQLDDLEmitter`.
- **Plugin Extensibility Guarantee**: Added `register_emitter(engine, emitter)` on `UniversalDDLAuthority`.

---

### P2.5 — Canonical Dependency Intelligence & Topological DDL Ordering
- **Canonical Dependency Planner ([`planner.py`](file:///a:/temp_akaal/akaal/schema/graph/planner.py))**: Implemented database-agnostic dependency authority `CanonicalDependencyPlanner` transforming `StructuredDDLArtifact` instances into a cycle-safe `DependencyPlan`.
- **Cycle Detection & Deferral**: Handled self-referencing foreign keys (`employees.manager_id -> employees.id`) and mutual foreign key cycles (`A ↔ B`) using Tarjan DFS cycle detection.
- **Parallel-Safe Execution Waves**: Grouped independent DDL operations into deterministic `ExecutionGroup` waves.

---

### P2.6 / P2.6.1 — Programmable Database Object Intelligence & Semantic Hardening
- **Canonical Programmable Authority ([`programmable_engine.py`](file:///a:/temp_akaal/akaal/schema/domain/programmable_engine.py))**: Implemented database-agnostic conversion engine `CanonicalProgrammableAuthority` generating `StructuredProgrammableArtifact` instances.
- **Lexical-Safe Masking Utility**: Implemented `SQLRulebook.mask_literals_and_comments()` to mask string literals (`'NVL(val)'`) and comments (`-- SYSDATE is used`) before applying expression replacements.
- **Execution Firewall Enforcement**: Added `is_auto_executable` flag on `StructuredProgrammableArtifact` (set to `False` for `MANUAL_REVIEW_REQUIRED` or `UNSUPPORTED`).

---

### P2.7 / P2.7.1 — Schema Comparison, Compatibility Intelligence, Risk Scoring & Drift Analysis
- **Canonical Comparison Authority ([`comparison_engine.py`](file:///a:/temp_akaal/akaal/schema/compatibility/comparison_engine.py))**: Established database-agnostic comparison authority `CanonicalSchemaComparator` comparing `CanonicalSchemaModel` vs `CanonicalSchemaModel` and producing strongly-typed `SchemaDifference` objects.
- **Explainable Risk Scorer (`CanonicalRiskScorer`)**: Implemented deterministic, evidence-derived risk assessment (`RiskAssessment`) computing bounded `risk_score` (0..100) and enforcing `is_safe_to_continue = False` whenever `BLOCKING` findings exist.
- **Deterministic Schema Drift Analyzer (`CanonicalDriftAnalyzer`)**: Built deterministic drift analyzer evaluating `SchemaDriftReport` against baseline SHA-256 schema fingerprints.

---

### P2.8 / P2.8.1 — Canonical Validation Engine, Serialization & Merkle Integrity
- **Canonical Value Serializer ([`physical_validator.py`](file:///a:/temp_akaal/akaal/validation/domain/physical_validator.py))**: Implemented versioned canonical serialization format (`SERIALIZATION_VERSION = AKAAL-CANONICAL-V1`).
- **Unambiguous Type Framing & Normalization**: Format `[TYPE_CAT_LEN]:[TYPE_CAT]:[NULL_FLAG]:[VAL_BYTES_LEN]:[VAL_BYTES]`. Handles `NULL`, `Decimal` normalization (`100.00` == `100`), float special values (`NaN`, `+Inf`, `-Inf`), UTF-8 Unicode NFC normalization, Oracle empty string semantics (`""` -> `NULL`), tz-aware datetime to UTC ISO8601, UUID bytes, JSON deterministic key ordering (`sort_keys=True`), and exact binary hashing.
- **LOB Streaming Hash Capability**: Added `hash_lob_stream(chunk_iterable)` for streaming large LOBs without loading entire payloads into RAM.
- **Framed Row Serialization & Merkle Tree**: Prevents field boundary concatenation collisions (`["ab", "c"]` != `["a", "bc"]`). Computes SHA-256 Merkle root trees.

---

### P2.9 / P2.9.1 — Validation-Only Mode, Deep Reconciliation & Semantic Hardening
- **Validation-Only Execution Mode ([`reconciliation.py`](file:///a:/temp_akaal/akaal/validation/domain/reconciliation.py))**: Implemented `ValidationExecutionMode.VALIDATION_ONLY` for independent validation of pre-existing source and target databases without requiring AKAAL migration history.
- **Validation-Only Write Firewall (`ValidationOnlyWriteFirewall`)**: Enforces `ValidationWriteFirewallError` if target mutation operations (`INSERT`, `UPDATE`, `DELETE`, `TRUNCATE`, `DDL`) are attempted during `VALIDATION_ONLY` mode.
- **Universal Deep Reconciliation Engine (`CanonicalReconciliationEngine`)**: Hierarchical progressive narrowing (`Table -> Merkle Root -> Chunk -> Key Range -> Row -> Column`). Pinpoints `MATCHED`, `SOURCE_ONLY`, `TARGET_ONLY`, `VALUE_MISMATCH`, `UNSUPPORTED`, `INDETERMINATE`, `ERROR` row classifications and pinpoints exact mismatched columns (`ColumnDifference`).

---

### P2.10 / P2.10.1 — Reporting, Certification & Governance Evidence Hardening
- **Canonical Reporting Models ([`canonical_models.py`](file:///a:/temp_akaal/akaal/reporting/models/canonical_models.py))**: Implemented strongly-typed `CanonicalReport`, `CertificationArtifact`, `CertificationClaim`, `EvidenceManifestItem`, and `CanonicalReportType`.
- **Universal Backend Reporting Authority ([`canonical_reporting.py`](file:///a:/temp_akaal/akaal/reporting/engine/canonical_reporting.py))**: Aggregates frozen P2.2-P2.9 evidence (schema compatibility, risk assessment, physical Merkle checksums, deep row reconciliation, and governance approval gates) into versioned, tamper-evident `CanonicalReport` artifacts.
- **Cross-Job/Run Evidence Binding Firewall**: Hardened `generate_canonical_report()` to verify `reconciliation_evidence` is strictly bound to the target `job_id` and `run_id`, resisting evidence substitution attacks.
- **Secrets Redaction Firewall**: Implemented `_redact_secrets()` automatically redacting passwords, API tokens, and connection secrets from report metadata.

---

### P2.11 — Reports, Validation Results, Certification & Evidence UI
- **Migration Evidence Dossier Shell ([`ReportsModule.tsx`](file:///a:/temp_akaal/akaal_software/src/screens/ReportsModule/ReportsModule.tsx))**: Built enterprise React + TypeScript Reports UI consuming canonical backend truth (`CanonicalReportingAuthority`).
- **Reports History & Filter Bar**: Searchable, filterable enterprise data table displaying Report ID, Job/Run ID, Report Type, Source → Target, Created At, Outcome, and Certification Status (`CERTIFIED`, `CERTIFIED_WITH_WARNINGS`, `NOT_CERTIFIED`, `INDETERMINATE`).
- **Certification Hero Banner**: Highlights explicit certification outcome and explains failure causes (`NOT_CERTIFIED` or `INDETERMINATE`) with zero false-positive claims.
- **6-Tab Operational Dossier**: `Overview` (Executive metrics), `Validation` (Physical checksums & Merkle verification), `Reconciliation` (P2.9 mismatch intelligence), `Schema & Risk` (P2.7 risk findings & severity), `Evidence` (SHA-256 fingerprint, evidence manifest, `Verify Integrity`), `Governance` (Approval gate status).
- **Truth Firewall Enforcement**: Frontend renders backend truth directly (`NOT_CERTIFIED` can never render as `CERTIFIED`, redacted secrets `[REDACTED_SECRET]`, missing evidence rendered as `Unavailable`).
- **Extensible Export Architecture**: Actionable `JSON Report`, `JSON Certificate`, and `Markdown Dossier` exports. Ready for future PDF addition without UI layout redesign.
